---
page_title: "jamfpro_user"
description: |-
  Retrieves a Jamf Pro user using the Classic API `/JSSResource/users` endpoint. Supports lookup by `user_id`, `name`, or `email`, or listing all users with `list_all`. Exactly one lookup attribute must be provided. When `list_all` is used, only the `id` and `name` of each user are returned (the Jamf Pro list endpoint does not return full detail); single lookups return the full user object.
---

# jamfpro_user (Data Source)
Retrieves a Jamf Pro user using the Classic API `/JSSResource/users` endpoint. Supports lookup by `user_id`, `name`, or `email`, or listing all users with `list_all`. Exactly one lookup attribute must be provided. When `list_all` is used, only the `id` and `name` of each user are returned (the Jamf Pro list endpoint does not return full detail); single lookups return the full user object.

## Example Usage
```terraform
# Look up a single user by Jamf Pro ID (returns the full user object).
data "jamfpro_user" "by_id" {
  user_id = "1"
}

# Look up a single user by name (username).
data "jamfpro_user" "by_name" {
  name = "jappleseed"
}

# Look up a single user by email address.
data "jamfpro_user" "by_email" {
  email = "jappleseed@example.com"
}

# List all users (returns only the id and name of each user).
data "jamfpro_user" "all" {
  list_all = true
}

output "jamfpro_user_by_id_full_name" {
  value = data.jamfpro_user.by_id.items[0].full_name
}

output "jamfpro_user_count" {
  value = length(data.jamfpro_user.all.items)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `email` (String) The email address of the user to look up. Conflicts with other lookup attributes.
- `list_all` (Boolean) Retrieve all users in Jamf Pro. Returns only the `id` and `name` of each user. Conflicts with other lookup attributes.
- `name` (String) The name (username) of the user to look up. Conflicts with other lookup attributes.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))
- `user_id` (String) The Jamf Pro ID of the user to look up. Conflicts with other lookup attributes.

### Read-Only

- `id` (String) The unique identifier for this data source instance.
- `items` (Attributes List) The list of users matching the query criteria. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `custom_photo_url` (String) The custom photo URL for the user.
- `email` (String) The email of the user.
- `email_address` (String) The email address of the user.
- `enable_custom_photo_url` (Boolean) Whether a custom photo URL is enabled for the user.
- `extension_attributes` (Attributes List) The extension attributes of the user. (see [below for nested schema](#nestedatt--items--extension_attributes))
- `full_name` (String) The full name of the user.
- `id` (String) The unique identifier of the user.
- `ldap_server` (Attributes) The LDAP server associated with the user. (see [below for nested schema](#nestedatt--items--ldap_server))
- `links` (Attributes) The objects linked to the user. (see [below for nested schema](#nestedatt--items--links))
- `name` (String) The name (username) of the user.
- `phone_number` (String) The phone number of the user.
- `position` (String) The position (job title) of the user.
- `sites` (Attributes List) The sites associated with the user. (see [below for nested schema](#nestedatt--items--sites))

<a id="nestedatt--items--extension_attributes"></a>
### Nested Schema for `items.extension_attributes`

Read-Only:

- `id` (String) The ID of the extension attribute.
- `name` (String) The name of the extension attribute.
- `type` (String) The data type of the extension attribute.
- `value` (String) The value of the extension attribute.


<a id="nestedatt--items--ldap_server"></a>
### Nested Schema for `items.ldap_server`

Read-Only:

- `id` (String) The ID of the LDAP server.
- `name` (String) The name of the LDAP server.


<a id="nestedatt--items--links"></a>
### Nested Schema for `items.links`

Read-Only:

- `computers` (Attributes List) The computers linked to the user. (see [below for nested schema](#nestedatt--items--links--computers))
- `mobile_devices` (Attributes List) The mobile devices linked to the user. (see [below for nested schema](#nestedatt--items--links--mobile_devices))
- `peripherals` (Attributes List) The peripherals linked to the user. (see [below for nested schema](#nestedatt--items--links--peripherals))
- `total_vpp_code_count` (Number) The total number of VPP codes assigned to the user.
- `vpp_assignments` (Attributes List) The VPP assignments linked to the user. (see [below for nested schema](#nestedatt--items--links--vpp_assignments))

<a id="nestedatt--items--links--computers"></a>
### Nested Schema for `items.links.computers`

Read-Only:

- `id` (String) The ID of the linked object.
- `name` (String) The name of the linked object.


<a id="nestedatt--items--links--mobile_devices"></a>
### Nested Schema for `items.links.mobile_devices`

Read-Only:

- `id` (String) The ID of the linked object.
- `name` (String) The name of the linked object.


<a id="nestedatt--items--links--peripherals"></a>
### Nested Schema for `items.links.peripherals`

Read-Only:

- `id` (String) The ID of the linked object.
- `name` (String) The name of the linked object.


<a id="nestedatt--items--links--vpp_assignments"></a>
### Nested Schema for `items.links.vpp_assignments`

Read-Only:

- `id` (String) The ID of the linked object.
- `name` (String) The name of the linked object.



<a id="nestedatt--items--sites"></a>
### Nested Schema for `items.sites`

Read-Only:

- `id` (String) The ID of the site.
- `name` (String) The name of the site.